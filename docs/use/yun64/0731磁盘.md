以下是当前服务器各磁盘分区的空间和占用情况：

| 文件系统 | 总大小 | 已用 | 可用 | 使用率 | 挂载点 |
|---------|--------|------|------|--------|--------|
| overlay | 50G | 5.2G | 45G | 11% | `/` |
| /dev/vdc | 500G | 3.3G | 497G | 1% | `/data` |
| /dev/vdb | 2.0T | 829G | 1.2T | 42% | `/codev` |
| spaceclient | 1.0P | 0 | 1.0P | 0% | `/agentdrive/Anydev` |
| tmpfs (多个) | 64M~124G | 0 | 同左 | 0% | `/dev`、`/sys/fs/cgroup` 等 |

要点：
- **`/data`**（工作区所在盘）最大空间 500G，几乎空闲（仅用 1%）。
- **`/codev`** 是数据盘 2.0T，已用 829G（42%），剩余约 1.2T。
- 根文件系统 `/` 仅 50G，已用 11%，空间充足。
- 其他 `tmpfs` 均为内存临时文件系统，实际不占磁盘。

如果你想看某个目录下具体是哪些文件占了空间，可以告诉我，我帮你用 `du` 进一步分析（比如 `/data/workspace`）。