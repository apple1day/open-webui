I need to set up the startup logic to detect which models the user has available locally—they've got qwen2.5:14b, deepseek-r1:14b, qwen2.5:1.5b, and tinyllama, but no vision model yet. The script should list these existing models when it starts, then either let them proceed or prompt them to select or download a model if needed.
需求明确：启动时把本机已有模型**列出来**，能识别视觉模型就直接用；没有视觉模型时**列出现有模型让你选/或提示拉取**，而不是只告警就继续。先读取当前那段逻辑：
I'm going to parse the installed models from ollama, identify which ones support vision capabilities by checking for specific naming patterns like vl, llava, minicpm-v, and others, then either set the vision model if one's detected or display the full list for interactive selection.

I need to handle the macOS bash 3.2 limitation that doesn't support mapfile, so I'll use a while-read loop with an array instead. For vision detection, I'll use pattern matching against common vision model identifiers.

Now I'm building the model listing and detection logic: first I'll populate an array of installed models from ollama, display them to the user, then scan through to identify which ones support vision capabilities so I can offer that as a default option.

The selection flow depends on what's available—if a vision model is already configured and installed, use it directly; if one was detected, use that; otherwise prompt the user with options to either pull a new model, select from existing ones (with a warning that non-vision models won't process images), or skip and choose later in the interface. I'll export the chosen model as an environment variable for reference and set up a simple numbered menu for interactive selection when needed. Writing shell script logic... 完善数组展开的安全性检查，在遍历已安装模型时添加计数判断，同时检测是否有视觉模型已配置或自动识别兼容的视觉模型，最后在交互模式下也要加入相同的防护措施。
明白。我把「视觉模型检查」那段改造成：**先列出本机所有模型 → 自动识别视觉模型直接用 → 没有则交互式让你选（拉取/选已有模型/跳过）**，并兼容 macOS 自带 bash 3.2（避免空数组在 `set -u` 下报错）。
The user noticed the script appeared to hang at the backend startup step, but it actually progressed through — the model warning just delayed things before moving to step 2/3. The output confirms it reached step 3 and is likely waiting for the health check. I'm thinking about adding a final summary that displays which model was selected and potentially passing it to the frontend as a default, though the frontend already fetches models from the /models endpoint so it might not be necessary.

Let me add the chosen VISION_MODEL to the final ready banner and verify the syntax with a bash check.
语法校验：
Bash 3.2 is confirmed and my implementation handles it properly by avoiding mapfile and protecting against empty arrays. Now I need to update the final ready banner to display which model was selected so the user can see their choice.
确认本机就是 bash 3.2.57，脚本已按其兼容性写好（无 `mapfile`、空数组有保护），语法通过。顺便把最终就绪提示也带上选定的模型，方便你一眼看到：
改造完成。现在启动时的模型处理逻辑如下。

## 新行为

**1. 启动时先列出本机所有模型**（用你的环境举例会打印）：
```
[INFO] 本机已安装 4 个 Ollama 模型：
        • qwen2.5:14b
        • deepseek-r1:14b
        • qwen2.5:1.5b
        • tinyllama:latest
```

**2. 自动识别视觉模型**（名字含 `vl/llava/minicpm-v/moondream/vision/cogvlm` 等）：
- 找到 → 直接选用并继续；
- 你配置的 `VISION_MODEL` 已安装 → 直接用。

**3. 没有视觉模型时（你的情况）→ 交互式三选一**：
```
[WARN] 未检测到视觉模型 (qwen2.5-vl / llava / minicpm-v 等)，视频抽帧分析需要它。
  请选择操作：
    [1] 联网拉取推荐视觉模型: qwen2.5-vl:7b
    [2] 从已有模型中选一个 (非视觉模型无法识别画面, 仅供联调/文本汇总)
    [3] 跳过, 直接启动 (稍后在网页面板里选模型)
  输入序号 [默认 3]:
```
- 选 `2` 会再列出已有模型让你按序号选（比如先用 `qwen2.5:14b` 把整套流程跑通，但它看不了画面，只能做文本汇总联调）；
- 默认 `3` 直接启动，到网页面板里再选模型。

**4. 非交互/自动场景仍可用**：`PULL_MODEL=1 ./start-video-analysis.sh` 直接拉取；非终端环境则只提示不阻塞。

最终就绪横幅也会显示 `当前选用模型: xxx`。

## 重新运行
```bash
./start-video-analysis.sh
```

兼容性确认：你的 `/bin/bash` 是 3.2.57，脚本已规避该版本的空数组 `set -u` 报错和 `mapfile` 缺失问题，`bash -n` 通过。

> 提醒：要真正分析**视频画面**，仍需一个视觉模型。最省事是选 `[1]` 拉 `qwen2.5-vl:7b`；想更小可换 `ollama pull minicpm-v`，再用 `VISION_MODEL=minicpm-v ./start-video-analysis.sh`。